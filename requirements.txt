# Python Dependencies for Face Recognition System
# Install using: pip3 install -r requirements.txt

# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
tensorflow-lite-runtime>=2.12.0

# Alternative: Full TensorFlow (if TFLite runtime doesn't work)
# tensorflow>=2.12.0

# Optional but recommended
Pillow>=9.0.0
